# Meeting_Light

## Before you begin

Recommend to run the code in python virtual environment. Run the following commands in the terminal.

To create environment: `virtualenv meetinglight`

To open the environment:

For Mac: `source meetinglight/bin/activate`

For Windows: `meetinglight\Scripts\activate`

Also install dependencies. In terminal run: > pip install -r requirements.txt
